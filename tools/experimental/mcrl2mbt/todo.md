# TODO

Actually encode the multi-actions properly.

The "id" field is always just a number, what is the point of "r", "q" and "i" prefixes?

The I/O classification is very basic, needs to be extended to handle the lpsactionrename features.